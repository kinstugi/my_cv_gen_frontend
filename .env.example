# Base URL for the REST API (no trailing slash). Default is production; set only to use a different backend (e.g. local).
# VITE_API_BASE_URL=http://localhost:8080
# PDF extraction and tailoring both use Groq. Set Tailor__ApiKey in the backend/Render environment.
